#!/usr/bin/env zsh
set -euo pipefail

# find a subfolder and cd into it - requires 'tree' is installed
DIR=$(find . -mindepth 1 -maxdepth 4 -type d -print 2>/dev/null | fzf) \
  && cd "$DIR" || return
