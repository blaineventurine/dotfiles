#!/usr/bin/env zsh
set -euo pipefail

if [ ! -d "$HOME/.local/bin" ]; then
  mkdir -p "$HOME/.local/bin"
fi

# install_github_cli
install_nb
install_git_delta
install_fzf
install_rtx
install_ripgrep_all
install_marksman
install_vale
install_lazygit

